<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhenmei.testgson">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light">

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BaseActivity" />
        <activity
            android:name=".activity.BeanActivity"
            android:label="BeanActivity" />
        <activity
            android:name=".activity.JokeActivity"
            android:label="JokeActivity" />
        <activity
            android:name=".activity.ListBeanActivity"
            android:label="ListBeanActivity" />
        <activity
            android:name=".activity.ListMapActivity"
            android:label="ListMapActivity" />
        <activity
            android:name=".activity.ListStringActivity"
            android:label="ListStringActivity" />

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>
